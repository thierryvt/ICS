@model IEnumerable<Shared.Entities.Vrachtwagen>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Merk)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Type)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.TotaalKM)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DatumInDienst)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Merk)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Type)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.TotaalKM)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DatumInDienst)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.NummerPlaat }) |
            @Html.ActionLink("Details", "Details", new { id=item.NummerPlaat }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.NummerPlaat })
        </td>
    </tr>
}

</table>
